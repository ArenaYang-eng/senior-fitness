# 樂齡銀髮族 12 個月健身計畫 Web App — 交接指南

> 最後更新：2026-09-17　｜　版本：PWA 版（可安裝到手機桌面）

---

## 一、專案概述

社區健身房銀髮族專用的訓練計畫 Web App，訓練主軸為**預防肌少症與骨質疏鬆**。
使用對象是**沒接觸過健身器材的長者**，設計原則是「拿著手機邊看邊執行」。

**技術特性**

| 項目 | 說明 |
|---|---|
| 架構 | 純前端，無後端、無資料庫、無建置流程 |
| 相依套件 | 無（不使用任何框架或外部函式庫） |
| 字型 | 系統內建字型，無外部字型連線 |
| 資料儲存 | `localStorage`（存在使用者自己的裝置，不上傳） |
| 部署 | GitHub Pages 靜態託管 |
| PWA | 支援安裝到手機桌面、離線使用 |

**重要**：本 App 沒有任何伺服器端。使用者的測評、訓練紀錄**只存在他自己的手機裡**，
不會同步、不會回傳、管理員也看不到。若未來需要社區端統計，必須另外開發後端。

---

## 二、檔案結構

全部 7 個檔案，**必須放在 repository 的最外層（root）**，不要放進子資料夾。

```
/
├── index.html                 ← 主程式（HTML + CSS + JS 全部在這一個檔案）
├── manifest.json              ← PWA 設定（App 名稱、圖示、開啟方式）
├── sw.js                      ← Service Worker（離線支援）
├── icon-192.png               ← App 圖示
├── icon-512.png               ← App 圖示（大尺寸）
├── icon-maskable-512.png      ← Android 自適應圖示
└── apple-touch-icon.png       ← iOS 桌面圖示
```

日常內容維護**只會動到 `index.html`**，其他 6 個檔案通常不需要修改。

---

## 三、如何修改教學影片連結 ⭐

影片分成**兩個地方**，改法不同。用編輯器打開 `index.html`，以「尋找」功能定位。

### 3-1　器材教學影片（主要，共 5 台器材）

**位置**：搜尋 `const EQUIPMENT`（約在第 684 行），每台器材物件底下都有一個 `videos` 陣列。

**格式**：

```javascript
videos:[
  {title:'安全使用跑步機全攻略，跟著做就對啦！', channel:'康健雜誌', id:'_mez_sRxME0'},
  {title:'跑步機入門注意事項', channel:'i運動 isports', id:'Vf3TxicT50c'}
]
```

**三個欄位的意義**

| 欄位 | 說明 |
|---|---|
| `title` | 顯示在卡片上的影片標題 |
| `channel` | 顯示的頻道來源 |
| `id` | **YouTube 影片 ID**，系統會自動用它產生連結與縮圖 |

**如何取得 YouTube 影片 ID**

| 網址型態 | 影片 ID |
|---|---|
| `https://www.youtube.com/watch?v=abc123XYZ` | `abc123XYZ`（`v=` 後面那串） |
| `https://youtu.be/abc123XYZ` | `abc123XYZ`（最後那串） |
| `https://www.youtube.com/watch?v=abc123XYZ&t=30s` | `abc123XYZ`（`&` 之前為止，不要含 `&t=30s`） |

只填 ID，**不要填整串網址**。縮圖與連結都是程式自動組出來的：

```
連結 → https://www.youtube.com/watch?v={id}
縮圖 → https://img.youtube.com/vi/{id}/mqdefault.jpg
```

**常見操作**

換一支影片 → 改 `id`、`title`、`channel` 三個值即可。

新增一支影片 → 在 `]` 之前加一行，**注意上一行結尾要補逗號**：

```javascript
videos:[
  {title:'原本的影片', channel:'某頻道', id:'AAAAA'},   ← 這裡要有逗號
  {title:'新增的影片', channel:'新頻道', id:'BBBBB'}    ← 最後一行不要逗號
]
```

刪除一支影片 → 整行刪掉，並確認剩下的最後一行結尾**沒有**逗號。

某台器材不放影片 → 整個 `videos:[...]` 區塊刪掉即可，程式會自動不顯示影片區（見 `videoCardsHtml()`）。

### 3-2　安全守則頁的平衡訓練影片（1 支）

**位置**：搜尋 `K84qprMH1Og`（約在第 587、589 行）。

這一支是直接寫死在 HTML 裡的，**同一個影片 ID 出現兩次，兩處都要改**：

```html
<a class="video-card" href="https://www.youtube.com/watch?v=K84qprMH1Og" target="_blank" rel="noopener">
  <div class="video-thumb">
    <img src="https://img.youtube.com/vi/K84qprMH1Og/mqdefault.jpg" alt="年長者的平衡訓練" loading="lazy">
    <span class="play">▶</span>
  </div>
  <div class="video-info">
    <div class="vt">年長者的平衡訓練！三招增進平衡感</div>   ← 標題
    <div class="vc">北木健身 · YouTube</div>                ← 頻道
  </div>
</a>
```

### 3-3　目前收錄的影片清單

| 位置 | 標題 | 頻道 | 影片 ID |
|---|---|---|---|
| 跑步機 | 安全使用跑步機全攻略 | 康健雜誌 | `_mez_sRxME0` |
| 跑步機 | 跑步機入門注意事項 | i運動 isports | `Vf3TxicT50c` |
| 橢圓機 | 3招正確使用滑步機 | 康健雜誌 | `eN6dFAnxVWM` |
| 靠背式健身車 | 健身房設備教學｜斜臥式腳踏車 | 捷寶雲品 | `8Gzs5X_W3zQ` |
| 雙滑輪機 | 動作教學：滑輪坐姿划船 | 前勁體能 | `sJOEc7HP1FE` |
| 雙滑輪機 | 動作教學：滑輪下拉 | 前勁體能 | `UEjioUYFARU` |
| 啞鈴 | 年長者也能開始的啞鈴訓練 | Kopi | `ldTnHnS6EDQ` |
| 啞鈴 | 長者運動：簡易新手啞鈴訓練 | GymBeginner | `HHv2OAeJMWs` |
| 安全守則頁 | 年長者的平衡訓練！三招增進平衡感 | 北木健身 | `K84qprMH1Og` |

### 3-4　影片維護注意事項

- 這些是**第三方公開影片**，不是社區自製，內容與器材品牌不一定完全一致。
  頁面上已標註「僅供參考，實際操作以現場教練說明為準」。
- **影片可能失效**（頻道下架、影片轉私人）。系統**沒有自動偵測失效的機制**，
  建議每季人工點過一輪。失效的徵兆是縮圖變成灰底或點進去顯示「無法播放」。
- 若未來改用社區自製影片，只要上傳到 YouTube（可設為「不公開」，有連結才能看），
  取得 ID 後照上述方式替換即可，程式不用改。

### 3-5　修改後如何驗證

改完先在本機用瀏覽器打開 `index.html` 檢查：

1. 頁面能正常顯示（若整頁空白或版面壞掉 → JS 語法錯誤）
2. 「器材動作指南」頁的影片縮圖有出現（沒出現 → 影片 ID 填錯）
3. 點縮圖能正確跳到 YouTube

**最常見的兩個語法錯誤**

- 引號用到**中文全形引號**（`'`）→ 必須用英文半形 `'`
- 逗號位置錯誤 → 每行結尾要有逗號，**最後一行不要**

---

## 四、其他內容如何修改

所有內容資料都集中在 `index.html` 的 `/* ---- DATA ---- */` 區塊（約第 621 行起）。

| 要改什麼 | 改哪裡 | 說明 |
|---|---|---|
| 分級名稱、每週目標次數、階段說明 | `LEVELS`（第 623 行） | `target` 是每週目標訓練次數 |
| 三階段目標、頻率、重點、里程碑 | `PHASES`（第 629 行） | 對應「12個月計畫」頁 |
| 每週課表（7 天內容） | `WEEK`（第 654 行） | 三個等級各一組，**必須維持 7 天、順序為一～日** |
| 器材說明、安全提醒、各級劑量 | `EQUIPMENT`（第 684 行） | `levels` 三個 key 必須齊全 |
| 慢性病選項 | `CONDITIONS`（第 740 行） | 測評表的複選項目 |
| 安全守則內容 | 直接搜尋 `id="screen-safety"` | 寫死在 HTML |

**修改 `WEEK` 的注意事項**：`type` 欄位會決定顯示的圖示與行為，可用值為
`cardio`（❤️有氧）、`strength`（💪肌力）、`active`（🚶主動恢復）、`rest`（😴休息，不可記錄）。

---

## 五、分級判定邏輯（重要，涉及安全）

**這是最需要專業審核的部分**，目前門檻為產品原型值。

**判定位置**：`analyzeAssessment()`（第 841 行）與 `getSuggestedLevel()`（第 866 行）

**現行規則**

1. **月份決定上限，測評決定能否達到上限**——兩者取較低者。
   避免「時間到就自動升級」或「一次測驗好就跳到高階」兩種風險。

2. **三項核心測試達標門檻**（目前值）

   | 測試 | 達標門檻 |
   |---|---|
   | 30 秒坐站 | ≥ 12 次 |
   | 起立走 TUG | ≤ 10 秒 |
   | 單腳站立 | ≥ 10 秒 |

   達標 3 項 → 高階；2 項 → 中階；0–1 項 → 初階。

3. **紅旗條件（直接鎖定初階，不受月份影響）**
   - 未勾選「已獲醫師同意運動」
   - 單腳站立 < 5 秒（跌倒風險）
   - TUG > 20 秒（移動功能不足）

4. **體前彎與握力只作紀錄，不計分**——刻意設計，避免「有填就送分」灌高等級。

**待辦**：上述門檻應由復健科或體適能專業人員，依社區實際長者族群的常模重新校準後再正式推廣。

---

## 六、資料結構

儲存於 `localStorage`，key 為 `senior_fitness_app-data`。

```javascript
{
  currentLevel: 'beginner' | 'intermediate' | 'advanced',
  startDate: '2026-01-15',          // 計畫開始日，null 表示尚未設定
  assessments: [                     // 測評紀錄，依日期排序（累積，不覆蓋）
    {
      id, type,                      // baseline / checkin / final
      date, age, conditions[], approved, notes,
      tests: { stand, tug, balance, reach, grip },
      wellbeing
    }
  ],
  sessionRecords: {                  // 訓練紀錄，以日期為 key
    '2026-01-15': { level, day, rpe, pain, ts }
  }
}
```

**設計要點**：`sessionRecords` 以**實際日期**為 key（非星期），所以可以回頭補記前幾天，
歷史紀錄也不會因為換等級而消失。`assessments` 是陣列累積，**不可改成覆蓋**，
否則前後測比較功能會失效。

---

## 七、部署流程

### 更新內容

1. 修改 `index.html`
2. GitHub repository → `Add file` → `Upload files` → 拖入新檔案（覆蓋同名檔）
3. `Commit changes`
4. 等約 1 分鐘生效

**網址不會改變**，LINE 群組裡的舊連結仍然有效，長輩不需要重新加。

### Service Worker 快取策略

`sw.js` 採**網路優先（network-first）**：有網路時一律抓最新版並更新備份，沒網路才用備份。
因此更新內容後長輩會拿到新版，**不會有「改了看不到」的快取問題**。

若未來改動 `sw.js` 本身，記得把 `CACHE_NAME`（第 8 行）的版本號往上加
（例如 `senior-fitness-v1` → `v2`），舊快取才會被清掉。

### 本機測試限制

直接雙擊打開 `index.html` 可以測試大部分功能，但 **Service Worker 與 PWA 安裝提示不會運作**
（瀏覽器安全限制，需 HTTPS 或 localhost）。要完整測試請部署到 GitHub Pages 後用手機開啟。

---

## 八、已知限制與後續開發建議

### 已知限制

| 限制 | 影響 | 備註 |
|---|---|---|
| 資料只存單一裝置 | 換手機、清瀏覽器資料會全部消失 | 無後端，無法避免 |
| 無跨裝置同步 | 長輩用平板和手機會看到不同紀錄 | 同上 |
| 管理員看不到成效 | 無法做社區整體統計 | 需開發後端 |
| 影片失效無偵測 | 需人工定期檢查 | 見 3-4 |
| 分級門檻未校準 | 可能建議不適合的等級 | 見第五節，**優先處理** |
| iOS 無安裝橫幅 | iPhone 需手動「加入主畫面」 | Safari 限制，已在按鈕加說明 |

### 建議的後續開發方向

**優先度高**
- 分級門檻由專業人員依社區族群校準（安全相關）
- 加入「匯出紀錄」功能（產生文字或 CSV），讓長輩可以把成果傳給教練或子女，
  也可暫時彌補無後端的統計需求

**優先度中**
- 資料備份／還原（匯出 JSON 再匯入），解決換手機資料消失的問題
- 器材動作的分解圖示或步驟卡（部分長輩不方便在健身房播影片）
- 列印版課表（給不使用智慧型手機的長輩）

**優先度低**
- 後端帳號系統與社區端儀表板（需評估個資保護與維運成本）
- 教練端介面，可遠端調整個別長輩課表

---

## 九、維護檢查清單

**每季**
- [ ] 逐一點開所有教學影片，確認未失效
- [ ] 檢查 GitHub Pages 網址仍可正常開啟

**每年或有專業人員參與時**
- [ ] 檢視分級門檻是否符合實際長者族群
- [ ] 檢視課表內容與器材清單是否與現場相符（器材汰換時務必同步更新）

---

## 十、免責聲明（已內建於 App）

- 器材資訊依據社區提供的「社區健身房器材功能與特色指南」。
- 四項功能性測試為業界常用的銀髮族體適能評估項目，但**自動分級門檻為產品原型**，
  正式導入前應由合格運動或醫療專業人員校準審核。
- 教學影片為第三方公開內容，非社區自製，實際操作以現場教練說明為準。
- 訓練與評估內容為方案設計，個別長輩仍應依醫療專業人員建議調整。
